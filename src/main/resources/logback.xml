<configuration scan="true" scanPeriod="60 seconds">

	<contextName>${contextPath}</contextName>
	
	<appender name="FILE"
		class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>../logs/${HOSTNAME}-${contextPath}.log</file>

		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<!-- daily rollover -->
			<fileNamePattern>../logs/${HOSTNAME}-${contextPath}.%d{yyyy-MM-dd}.log
			</fileNamePattern>
			<!-- keep 30 days' worth of history capped at 1GB total size -->
			<maxHistory>30</maxHistory>
			<totalSizeCap>1GB</totalSizeCap>
		</rollingPolicy>
		<encoder>
			<pattern>%date %level [%thread] %logger{64} [%file:%line] %msg%n
			</pattern>
		</encoder>
	</appender>

	<appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<pattern>%d ${contextPath} %date %level [%thread] %logger{64}
				[%file:%line] %msg%n</pattern>
		</encoder>
	</appender>

	<logger name="com.smeup.provider.log.LoggedInterceptor" level="DEBUG" />

	<root level="INFO">
		<appender-ref ref="FILE" />
		<appender-ref ref="STDOUT" />
	</root>
</configuration>
